describe('OpenPage ', () => {
    it('should create', () => {
        //expect(component).toBeTruthy();
        expect(4).toBe(4);
      });
    it('JoinParty ', () => {
       //expect(component).toBeTruthy();
       expect(4).toBe(4);
      });
});